# タスクリスト：ガイドライン改善

## タスク一覧

### フェーズ 1: CLAUDE.md の更新

- [x] **T-1**: 技術スタックの修正（IP-1, IP-9a）
  - `framer-motion` → `motion` に変更
  - Tailwind CSS v4 に関する補足（`tailwind-merge` 互換性の注記）を追加
  - **完了条件**: 技術スタック欄が最新のパッケージ名・v4 前提の記述になっている

- [x] **T-2**: 環境要件セクションの新規追加（IP-4）
  - Node.js / pnpm のバージョン要件を記載
  - WSL / devcontainer 環境の注意事項を記載
  - **挿入位置**: 「shadcn/ui MCP サーバーの活用」セクションの前
  - **完了条件**: 環境要件セクションが追加されている

- [x] **T-3**: MCP 設定の具体化（IP-5）
  - Claude Code 向け設定例（`.mcp.json`）を追加
  - Cursor 向け設定例（`.cursor/mcp.json`）を追加
  - **挿入位置**: 「shadcn/ui MCP サーバーの活用」セクション内
  - **完了条件**: 両環境の設定例が記載されている

- [x] **T-4**: コンポーネント構成ルールの修正（IP-2, IP-9b）
  - 「初期段階」と「Storybook 導入後」の 2 段階に分割
  - 初期段階: `.stories.tsx` と `README.md` を必須から除外
  - README は成熟コンポーネントに対して作成する方針を明記
  - JSDoc コメントでの代替を記載
  - **完了条件**: 2 段階の構成ルールが定義されている

- [x] **T-5**: リポジトリ全体構造の修正（IP-1, IP-2, IP-7）
  - `tailwind.config.ts` を削除
  - `src/app/globals.css` を追加（役割を注記）
  - `stories/` ディレクトリの説明を「将来導入」に修正
  - `docs/CATALOG.md` の説明を「自動生成推奨、補助的位置づけ」に修正
  - **完了条件**: リポジトリ構造が Tailwind v4 / Storybook 段階的導入 / CATALOG.md 方針と整合している

- [x] **T-6**: コーディング規約の修正（IP-9c）
  - インポート順序セクションに自動化ツール（ESLint `import/order` 等）の推奨を追記
  - **完了条件**: 自動化手段が記載されている

- [x] **T-7**: テスト戦略セクションの新規追加（IP-3）
  - Vitest + React Testing Library を明記
  - テスト粒度（レンダリング / インタラクション / アクセシビリティ）を定義
  - axe-core による自動チェックを推奨
  - カバレッジ方針を記載
  - **挿入位置**: 「コーディング規約」の後
  - **完了条件**: テスト戦略セクションが追加されている

- [x] **T-8**: コミットメッセージ規約の拡張（IP-8）
  - `feat` 以外のプレフィックス（`fix`, `refactor`, `docs`, `chore`, `test`）を追加
  - コンポーネント以外の変更パターンを定義
  - **完了条件**: 全プレフィックスが定義されている

- [x] **T-9**: shadcn/ui コンポーネント更新手順の追加（IP-6）
  - 「よくある判断基準」セクションに更新手順を追加
  - **完了条件**: diff → overwrite → 影響確認 → 検証の手順が記載されている

- [x] **T-10**: 品質チェックリストの修正（IP-2, IP-7）
  - CATALOG.md 項目を「カタログページに反映されていること」に変更
  - Storybook 関連の記述があれば条件付きに修正
  - **完了条件**: 品質チェックリストが他の改善内容と整合している

### フェーズ 2: 関連ドキュメントの同期・更新

- [x] **T-11**: `.cursor/rules/project-memory.mdc` の同期
  - CLAUDE.md の `## 概要` 以降の本文を同期
  - フロントマター・タイトル・同期注意書きは維持
  - **完了条件**: CLAUDE.md と本文が一致している

- [x] **T-12**: `docs/product-requirements.md` の更新
  - セクション 3「コンポーネント提供形態」: Storybook を「将来導入」に変更
  - AC-1: `.stories.tsx` を初期段階の必須から除外
  - AC-4: CATALOG.md を補助的位置づけに変更
  - NFR-3: テストフレームワーク（Vitest + RTL）を明記
  - **完了条件**: ガイドラインとの矛盾が解消されている

### フェーズ 3: 最終確認

- [x] **T-13**: 全体整合性チェック
  - CLAUDE.md 内のセクション間で矛盾がないか確認
  - CLAUDE.md と `project-memory.mdc` の同期が完全か確認
  - CLAUDE.md と `product-requirements.md` の整合性を確認
  - **完了条件**: 3 ドキュメント間で矛盾がない
